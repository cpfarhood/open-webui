---
# Database credentials for CloudNativePG cluster
# PLACEHOLDER - needs to be sealed with actual values
apiVersion: v1
kind: Secret
metadata:
  name: open-webui-db-credentials
  namespace: open-webui
type: Opaque
stringData:
  username: openwebui
  password: REPLACE_WITH_STRONG_PASSWORD
  uri: postgresql://openwebui:REPLACE_WITH_STRONG_PASSWORD@open-webui-postgres-rw.open-webui.svc.cluster.local:5432/openwebui
---
# S3 backup credentials for PostgreSQL backups
# PLACEHOLDER - needs to be sealed with actual values
apiVersion: v1
kind: Secret
metadata:
  name: open-webui-s3-backup-credentials
  namespace: open-webui
type: Opaque
stringData:
  access_key: REPLACE_WITH_CEPH_ACCESS_KEY
  secret_key: REPLACE_WITH_CEPH_SECRET_KEY
